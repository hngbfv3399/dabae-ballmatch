# 📑 시즌 4(v4.0.0) 대공사 진행 가이드 & 작업 규칙

본 문서는 시즌 4 업그레이드 작업을 여러 번의 대화(턴)에 걸쳐 안전하고 토큰 효율적으로 수행하기 위한 진행 가이드이자 규칙 문서입니다. 
작업을 진행하는 AI 에이전트는 **반드시 이 문서를 가장 먼저 읽고 진행 상황을 업데이트**해야 합니다.

---

## 🛠️ AI 자체 작업 규칙 (토큰 절약 및 안전성 확보)

1. **상대적 코드 변경 (Replace Chunk 최소화)**:
   - `src/main.ts`는 용량이 매우 큽니다. 한 번에 전체를 수정하려고 하지 말고, 수정이 필요한 특정 UI 렌더링 함수나 리스너 부분만 타겟팅하여 부분 교체(`replace_file_content` 또는 `multi_replace_file_content`)하십시오.
2. **독립 모듈화 지향**:
   - 새로 추가되는 UI 스타일이나 독립된 비즈니스 로직(예: 7대 스탯 계산기, 스킬 트리 정의 등)은 `src/main.ts` 안에 모두 때려 넣지 말고, 별도 헬퍼 파일(예: `src/maingame/statsCalculator.ts`)을 파서 내보내기/가져오기 하십시오.
3. **스키마 인덱스 필수 준수**:
   - Convex 쿼리 작성 시 반드시 `withIndex`를 명시하고 스키마에 정의된 인덱스 구조로만 필터링하십시오. (인덱스 미지정 시 테이블 전체 스캔으로 속도 저하 발생)
4. **점진적 배포 및 빌드 체크**:
   - 한 단계를 완료할 때마다 반드시 로컬에서 `npx tsc` 또는 `npm run build`를 돌려 TypeScript 컴파일 에러를 즉시 검출하고 해결한 뒤 다음 단계로 가십시오.

---

## 📊 진행 상황 체크리스트

### 🏁 Phase 1. DB 스키마 재정의 및 리셋
- [ ] `convex/schema.ts` 수정 (기존 테이블 정리 및 닉네임 제거, `characterProgress` 계정화)
- [ ] 개발(Dev) DB 및 프로덕션(Prod) DB의 모든 테이블 완전 비우기(Wipe)
- [ ] `npx convex dev --once` 및 `npx convex deploy`로 스키마 강제 동기화

### 🏁 Phase 2. 캐릭터 계정 및 도감 초기 데이터 시딩
- [ ] `convex/persistentItems.ts` 내 20종 캐릭터 계정(1레벨, 1000코인, 0xp) 선행 생성 시딩 mutation 구현
- [ ] 도감용 영구 아이템 4종 메타데이터 시딩 구현
- [ ] 시딩 뮤테이션 실행 및 DB 정상 주입 확인

### 🏁 Phase 3. 로그인 및 재화 수수료 API 구현
- [ ] `convex/progression.ts` 수정: `getOrCreate` 대신 `getCharacterProgress` 구현 (존재하지 않으면 동적 생성하지 않고 null 반환)
- [ ] 프론트엔드 로그인 검증용 API 테스트
- [ ] PvP/던전 클리어 시 결과에 따라 코인을 증가시키는 `rewardCoins` 뮤테이션 구현

### 🏁 Phase 4. 상점/성장/스킬 트리 코어 로직 구현
- [ ] **가챠**: 코인 차감 후 `playerItems` 및 `cosmeticUnlocks`에 획득물 추가 구현
- [ ] **성장(제물 강화)**: 재료 장비를 삭제하고 대상 장비의 XP/Level을 증가시키는 `feedItem` 구현
- [ ] **성장(장비 판매)**: 장비를 삭제하고 등급 비례 코인을 환급하는 `sellItem` 구현
- [ ] **성장(8슬롯 장착)**: `equippedSlot` 범위(1~8) 설정 및 장착/해제 로직 작성
- [ ] **스킬 트리**: 스킬 포인트(SP)를 사용해 `characterSkills` 레벨을 올리는 `investSkillPoint` 구현

### 🏁 Phase 5. 하단 탭 내비게이션 및 로비 UI 대수술
- [ ] 로비 하단 고정식 내비게이션 바 구현 (HTML/CSS Flex 적용)
- [ ] 캐릭터 이름 로그인 모달 UI 구현 (미인증 계정 시 `"현재 해당 캐릭터가 없으니 운영자에게 문의 하세요"` 에러 얼럿 출력)
- [ ] **플레이 탭**: 캐릭터 상태, 8개 장착 장비 그리드 요약, 플레이 버튼 연동
- [ ] **상점 탭**: 가챠 뽑기 연출 및 결과 창 UI 구현
- [ ] **스킨 탭**: 캐릭터 스킨 착용 상태 프리뷰 + 보유 스킨 장착/해제 UI 구현
- [ ] **성장 탭**: 장비 20칸 인벤토리 UI + 합성 강화 모달 + 스킬 트리 포인트 클릭 UI 구현
- [ ] **도감 탭**: 캐릭터 7대 종합 스탯 수치(기본치 + 장비 보정) 표기 패널 구현

### 🏁 Phase 6. 전투 물리 엔진 연동 및 밸런싱
- [ ] `src/main.ts` 내 볼 생성/충돌 로직에 캐릭터의 7대 종합 스탯 적용
- [ ] 빌드 에러 전수 조사 및 해결 (`npm run build` 통과)
- [ ] 실서버 배포 및 `PATCH_NOTES.md` 작성
